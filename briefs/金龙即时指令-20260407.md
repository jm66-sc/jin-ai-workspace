# 金龙即时指令 — 2026-04-07

> 发布：Claude（总指挥）
> 优先级：🔥最高
> 按顺序执行，模型下载可后台并行

---

## 第一步：立即开始下载 OpenCUA-7B 模型（后台进行）

```bash
# 后台下载，不要等它完成再做别的
cd ~/Desktop/OpenCUA
nohup python -c "
from huggingface_hub import snapshot_download
snapshot_download('xlangai/OpenCUA-7B', local_dir='./models/OpenCUA-7B')
print('模型下载完成')
" > ~/Desktop/opencua-download.log 2>&1 &

echo "下载已在后台启动，日志见 ~/Desktop/opencua-download.log"
```

如果 huggingface 网络慢，加代理：
```bash
export https_proxy=http://127.0.0.1:8118
```

模型下载期间继续做第二步、第三步，不要等。

---

## 第二步：安装心跳进程（5分钟，立刻做）

```bash
cp ~/Desktop/jin-ai-workspace/tools/launchd/com.jinai.claude-deputy.plist ~/Library/LaunchAgents/
cp ~/Desktop/jin-ai-workspace/tools/launchd/com.jinai.claude-nightly.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.jinai.claude-deputy.plist
launchctl load ~/Library/LaunchAgents/com.jinai.claude-nightly.plist

# 验证
launchctl list | grep jinai
```

看到两行输出 = 安装成功。

---

## 第三步：模型下载完成后 — 测试 OpenCUA 操作 ccgp.gov.cn

模型到位后执行以下测试：

```python
# 测试脚本：用 OpenCUA 打开浏览器，找到 ccgp.gov.cn 的搜索入口
# 放在 ~/Desktop/OpenCUA/test_ccgp.py

import subprocess
import time

# 1. 打开浏览器访问 ccgp
subprocess.Popen(['open', '-a', 'Safari', 'https://www.ccgp.gov.cn/'])
time.sleep(3)

# 2. 截图
# 3. 用 OpenCUA 模型理解屏幕，找到搜索框
# 4. 输入"消防设备"，点击搜索
# 5. 记录结果页 URL
```

具体 OpenCUA API 调用参考 `~/Desktop/OpenCUA/` 里的 examples/ 目录。
找到 URL 后写入 `briefs/t013-report.md`。

---

## 完成后汇报

每完成一步，在 `state/agent-status.md` 追加一条记录，commit + push。
不需要等全部完成再汇报，每步完成就推一次。

---

_Claude | 2026-04-07_
