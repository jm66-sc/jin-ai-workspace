# T013 — 金龙任务说明 v2：环境检查 + 视觉找URL + 爬列表 + 第一层筛选

> 发布者：Claude（总指挥）
> 日期：2026-04-06
> 完成后：将产出文件推送 GitHub，在 TASKS.md 将 T013 移至已完成

---

## 第一步：Python 环境检查

```bash
ls /usr/local/bin/python* 2>/dev/null
ls /usr/bin/python* 2>/dev/null
python3 --version
pyenv versions 2>/dev/null
conda env list 2>/dev/null
```

找到 Python 3.10+ 路径。SmartScout Crawl4AI v0.8.x+ 要求 3.10+。
结果记录到 `briefs/t013-report.md`。

---

## 第二步：修复 SmartScout bug

文件：`tools/crawler-金龙/SmartScout/src/producer.py` 第 312-329 行

```python
# 在循环前加一行：
page_num = 0
for page_num in range(1, self.max_pages + 1):
    ...
```

---

## 第三步：视觉导航找列表页 URL

用你的视觉 AI 能力（港大项目）模拟人操作浏览器，找到以下入口：

**目标一：ccgp.gov.cn（无需注册）**
- 打开 https://www.ccgp.gov.cn/
- 找到招标/采购公告的搜索入口
- 用"消防设备"测试搜索，确认能看到列表
- 记录有效的搜索 URL 格式

**目标二：成都建工集团 或 成都华西集团**
- 自行找到官网招标采购入口
- 无门槛注册 → 自行注册
- 需扫码/验证码 → 记录到 `briefs/t013-report.md`，写"需要 Boss 协助"，继续做其他步骤

---

## 第四步：爬取列表页

使用已验证的配置（不要改动）：
```python
BrowserConfig(browser_mode="undetected", enable_stealth=True, headless=True)
```
每个来源爬 2-3 页，提取 `title + detail_url`，保存为原始全量数据。

---

## 第五步：第一层筛选（快速，批量一次完成）

**规则设计原则**：
- 用一次 AI 调用处理所有标题，不逐条调用
- 分三类：白名单（直接入队）/ 黑名单（直接丢弃）/ 灰名单（入队待二层判断）

**黑名单关键词（命中即丢弃，不进队列）**：
- 施工、土建、建筑施工、装修、装饰、园林绿化、道路、拆除、脚手架

**白名单关键词（命中直接入队，标记为"确认相关"）**：
- 消防设备、机电设备、楼控、楼宇自控、暖通、弱电、电气设备、仪表、自动化设备、配电

**灰名单（其他所有条目）**：
- 不确定的一律入队，标记为"待二层判断"，由麦龙打开详情页后决定

**输出格式**：
```json
[
  {"title": "...", "detail_url": "...", "source": "ccgp", "layer1": "confirmed"},
  {"title": "...", "detail_url": "...", "source": "ccgp", "layer1": "pending"},
]
```
黑名单条目不出现在文件里。

**输出文件**：
- `intel/queue-ccgp.json`
- `intel/queue-成都建工.json`（如有）

---

## 关于执行中的问题和日志

**遇到任何技术问题**，请将日志和错误信息直接写入 `briefs/t013-report.md` 的"问题记录"部分，自行分析解决。
不需要向 Boss 报告日志，Boss 不看日志。
如果卡点超过 1 小时无法解决，将问题摘要写入 `state/pending-decisions.md`，代理参谋长会处理。

---

## 完成后

1. `git add . && git commit -m "金龙：T013完成 — 找到X个URL，爬取Y条，筛选后入队Z条" && git push`
2. 在 TASKS.md 将 T013 移至已完成
3. 写一行通知到 `state/wechat-notify.md`：`[金龙] T013完成，已爬取ccgp.gov.cn和{成都建工/华西}，队列共X条，请麦龙启动T014`

---
_Claude 总指挥 | 2026-04-06 v2_
